From 57473d34dc396d839664ce582793fc0e300f724d Mon Sep 17 00:00:00 2001
From: "song.yu" <song.yu@sophgo.com>
Date: Mon, 4 May 2026 09:24:33 +0800
Subject: [PATCH] feat(panel): generate configs from PanelSupportList

Change-Id: I081bcff4b8d3053e5b6b463c65795a820935c8c8
---
 alios.mk                                      |  2 +
 .../cv184x/cv184x_fpga/cv184x_fpga_defconfig  |  2 +-
 .../cv184x_palladium_defconfig                |  2 +-
 envsetup_soc.sh                               |  7 +-
 media/.gitignore                              |  3 +-
 panels/panel_list.json                        | 40 ---------
 scripts/boards_scan.py                        |  3 -
 scripts/build_helper.py                       |  2 +-
 scripts/gen_panel_config.py                   | 86 ++++---------------
 9 files changed, 31 insertions(+), 116 deletions(-)
 delete mode 100644 panels/panel_list.json

diff --git a/alios.mk b/alios.mk
index 2c524932..61f1663c 100644
--- a/alios.mk
+++ b/alios.mk
@@ -20,6 +20,7 @@ endef
 alios-depends:
 	$(call print_target)
 	${Q}ln -sf $(TOP_DIR)/build/media/SensorSupportList $(TOP_DIR)/cvi_alios/components/cvi_mmf_sdk/
+	${Q}ln -sf $(TOP_DIR)/build/media/PanelSupportList $(TOP_DIR)/cvi_alios/components/cvi_mmf_sdk/
 	${Q}cp -f $(MEDIA_INCLUDE_DIR)/internal/osdrv_uapi/* $(ALIOS_OSDRV_PATH)/include/common/uapi/
 	${Q}cp -f $(MEDIA_INCLUDE_DIR)/internal/comm/* $(ALIOS_OSDRV_PATH)/include/common/uapi/
 	${Q}cp -f $(MEDIA_INCLUDE_DIR)/release/cvi_defines.h $(ALIOS_OSDRV_PATH)/include/chip/$(CHIP_ARCH_L)/uapi/defines.h
@@ -50,6 +51,7 @@ alios_clean: alios-depends
 	${Q}rm -f $(ALIOS_MSG_PATH)/internal_include/msg/*.h
 	${Q}rm -rf $(ALIOS_MW_PATH)/include/isp
 #	${Q}rm -f $(TOP_DIR)/cvi_alios/components/cvi_mmf_sdk/SensorSupportList
+#	${Q}rm -f $(TOP_DIR)/cvi_alios/components/cvi_mmf_sdk/PanelSupportList
 	${Q}$(MAKE) -C ${ALIOS_SOLUTIONS_DIR} clean
 
 alios: alios-build
diff --git a/boards/cv184x/cv184x_fpga/cv184x_fpga_defconfig b/boards/cv184x/cv184x_fpga/cv184x_fpga_defconfig
index 03935d13..bc1f7881 100644
--- a/boards/cv184x/cv184x_fpga/cv184x_fpga_defconfig
+++ b/boards/cv184x/cv184x_fpga/cv184x_fpga_defconfig
@@ -4,7 +4,7 @@ CONFIG_TOOLCHAIN_GLIBC_ARM64=y
 CONFIG_BOOT_IMAGE_SINGLE_DTB=y
 CONFIG_STORAGE_TYPE_spinor=y
 CONFIG_SENSOR_SONY_IMX327_FPGA=y
-CONFIG_MIPI_PANEL_ILI9881C=y
+CONFIG_DSI_ILI9881C_720X1280_NULL_4LANE_60FPS=y
 CONFIG_UBOOT_2021_10=y
 CONFIG_KERNEL_SRC_5.10=y
 # CONFIG_ROOTFS_OVERLAYFS is not set
diff --git a/boards/cv184x/cv184x_palladium/cv184x_palladium_defconfig b/boards/cv184x/cv184x_palladium/cv184x_palladium_defconfig
index beab79d2..2c4f1087 100644
--- a/boards/cv184x/cv184x_palladium/cv184x_palladium_defconfig
+++ b/boards/cv184x/cv184x_palladium/cv184x_palladium_defconfig
@@ -5,7 +5,7 @@ CONFIG_BOARD_palladium=y
 CONFIG_BOOT_IMAGE_SINGLE_DTB=y
 CONFIG_STORAGE_TYPE_spinor=y
 CONFIG_SENSOR_SONY_IMX327_FPGA=y
-CONFIG_MIPI_PANEL_ILI9881C=y
+CONFIG_DSI_ILI9881C_720X1280_NULL_4LANE_60FPS=y
 CONFIG_UBOOT_2021_10=y
 CONFIG_KERNEL_SRC_5.10=y
 # CONFIG_ROOTFS_OVERLAYFS is not set
diff --git a/envsetup_soc.sh b/envsetup_soc.sh
index 2919cb1d..6b3875af 100755
--- a/envsetup_soc.sh
+++ b/envsetup_soc.sh
@@ -109,7 +109,6 @@ function _build_uboot_env()
   _build_fsbl_env
   export UBOOT_OUTPUT_FOLDER IMGTOOL_PATH FLASH_PARTITION_XML FIP_BIN_PATH
   export UBOOT_VBOOT RELEASE_VERSION PACK_BOOTLOGO STORAGE_TYPE COMPRESSOR_UBOOT
-  export PANEL_TUNING_PARAM PANEL_LANE_NUM_TUNING_PARAM PANEL_LANE_SWAP_TUNING_PARAM
 }
 
 function _build_busybox_env()
@@ -237,7 +236,7 @@ function _link_uboot_logo()
 {(
   print_notice "Run ${FUNCNAME[0]}() function"
   cd "$BUILD_PATH" || return
-  if [[ x"${PANEL_TUNING_PARAM}" = x"I80_panel_st7789v" ]]; then
+  if grep -q '^CONFIG_I80_ST7789V_240X320_NULL_60FPS_SW=y$' "$BUILD_PATH/.config"; then
     ln -sf "$COMMON_TOOLS_PATH"/bootlogo/logo_320x240.BMP "$COMMON_TOOLS_PATH"/bootlogo/logo.jpg
   fi
 )}
@@ -1271,6 +1270,10 @@ export TOP_DIR BUILD_PATH
 "${BUILD_PATH}/scripts/boards_scan.py" --gen-build-kconfig
 "${BUILD_PATH}/scripts/gen_sensor_config.py"
 "${BUILD_PATH}/scripts/gen_panel_config.py"
+if [[ -d "${TOP_DIR}/${UBOOT_SRC:-u-boot-2021.10}/drivers/video/cvitek" ]]; then
+  command cp -f "${BUILD_PATH}/output/Kconfig.panels" \
+    "${TOP_DIR}/${UBOOT_SRC:-u-boot-2021.10}/drivers/video/cvitek/Kconfig.panels"
+fi
 [[ -d "${TOP_DIR}/cvi_alios" ]] && ""${BUILD_PATH}/scripts/gen_pipeline_config.py""
 [[ -f "${TOP_DIR}/build/credential.sh" ]] && source "${TOP_DIR}/build/credential.sh"
 
diff --git a/media/.gitignore b/media/.gitignore
index c1db46b1..3e901bbf 100644
--- a/media/.gitignore
+++ b/media/.gitignore
@@ -1,4 +1,5 @@
 *.d
 *.o
 
-SensorSupportList
\ No newline at end of file
+SensorSupportList
+PanelSupportList
diff --git a/panels/panel_list.json b/panels/panel_list.json
deleted file mode 100644
index 000d4da6..00000000
--- a/panels/panel_list.json
+++ /dev/null
@@ -1,40 +0,0 @@
-{
-	"panel_list" : {
-		"mipi" : [
-			"hx8394",
-			"ili9881c",
-			"ili9881d",
-			"jd9366ab",
-			"nt35521",
-			"ota7290b",
-			"ota7290b_1920",
-			"icn9707",
-			"3aml069lp01g",
-			"st7701",
-			"st7785m"
-		],
-		"lvds" : [
-			"ek79202"
-		],
-		"i80" : [
-			"st7789v",
-			"st7789v3"
-		],
-		"bt" : [
-		],
-		"rgb" : [
-		]
-	},
-
-	"panel_param" : {
-		"lane_num" : [
-			"4_lanes",
-			"2_lanes",
-			"1_lane"
-		],
-
-		"lane_swap" : [
-			"lane_swap"
-		]
-	}
-}
diff --git a/scripts/boards_scan.py b/scripts/boards_scan.py
index fbb898a6..8ca94cdd 100755
--- a/scripts/boards_scan.py
+++ b/scripts/boards_scan.py
@@ -46,9 +46,6 @@ ENVS_FROM_CONFIG = [
     "FLASH_SIZE_SHRINK",
     "C906L_DMA_ENABLE",
     "BUILD_FOR_DEBUG",
-    "PANEL_TUNING_PARAM",
-    "PANEL_LANE_NUM_TUNING_PARAM",
-    "PANEL_LANE_SWAP_TUNING_PARAM",
     "MTRACE",
     "SKIP_UBOOT",
     "SKIP_UBOOT_DEBUG",
diff --git a/scripts/build_helper.py b/scripts/build_helper.py
index 89087653..a2f2bd37 100644
--- a/scripts/build_helper.py
+++ b/scripts/build_helper.py
@@ -25,7 +25,7 @@ BOARD_KCONFIG_SAVED_GLOB = join(BOARD_DIR, "*/*/*_defconfig")
 CHIP_LIST_PATH = join(BOARD_DIR, "chip_list.json")
 SENSOR_LIST_PATH = join(BUILD_REPO_DIR, "media/SensorSupportList/sensor_list/sensor_list.json")
 SENSOR_KCONFIG_PATH = join(BUILD_REPO_DIR, "output/Kconfig.sensors")
-PANEL_LIST_PATH = join(BUILD_REPO_DIR, "panels/panel_list.json")
+PANEL_HEADER_DIR = join(BUILD_REPO_DIR, "media/PanelSupportList/panels")
 PANEL_KCONFIG_PATH = join(BUILD_REPO_DIR, "output/Kconfig.panels")
 PIPELINE_KCONFIG_PATH = join(BUILD_REPO_DIR, "output/Kconfig.pipelines")
 
diff --git a/scripts/gen_panel_config.py b/scripts/gen_panel_config.py
index aeb14539..43708412 100755
--- a/scripts/gen_panel_config.py
+++ b/scripts/gen_panel_config.py
@@ -1,7 +1,7 @@
 #!/usr/bin/env python3
 # PYTHON_ARGCOMPLETE_OK
 
-import json
+import os
 import build_helper
 
 kconfig_tmpl = """
@@ -31,88 +31,40 @@ config {0}
       "y" Config {1}.
 """
 
-kconfig_config_str_tmpl = """
-config {1}
-    string{0}
-"""
 
-param_default_str_tmpl = """
-    default "{0}" if {1}"""
+def get_panel_headers():
+    panel_headers = []
 
+    for entry in os.listdir(build_helper.PANEL_HEADER_DIR):
+        panel_path = os.path.join(build_helper.PANEL_HEADER_DIR, entry)
+        if not os.path.isfile(panel_path):
+            continue
+        if not entry.endswith(".h") or entry == "panel_platform.h":
+            continue
+        panel_headers.append(os.path.splitext(entry)[0])
 
-def gen_panel_list(panel_intf_list):
-    kconfig_panel_list = ""
+    return sorted(panel_headers)
 
-    for panel_intf in panel_intf_list:
-        panel_list = panel_intf_list[panel_intf]
-        panel_intf = panel_intf.upper()
 
-        kconfig_panel_config_list = ""
-        for panel in panel_list:
-            panel_name_u = panel.upper()
-            panel_name_l = panel.lower()
+def gen_panel_list(panel_headers):
+    kconfig_panel_list = ""
 
-            kconfig_panel_config_list = (
-                kconfig_panel_config_list
-                + kconfig_config_bool_tmpl.format(
-                    panel_intf + "_PANEL_" + panel_name_u,
-                    panel_intf + "_panel_" + panel_name_l))
+    for panel in panel_headers:
+        kconfig_panel_list = (
+            kconfig_panel_list
+            + kconfig_config_bool_tmpl.format(panel.upper(), panel))
 
-        kconfig_panel_list = kconfig_panel_list + kconfig_panel_config_list
     kconfig_panel_list = kconfig_choice_tmpl.format("Panel selecting", kconfig_panel_list)
 
     return kconfig_panel_list
 
 
-def gen_panel_tuning_list(panel_intf_list, tuning_param):
-    kconfig_panel_tuning_list = ""
-    param_default_str = ""
-
-    for panel_intf in panel_intf_list:
-        panel_list = panel_intf_list[panel_intf]
-        panel_intf = panel_intf.upper()
-
-        for panel in panel_list:
-            panel_u = panel.upper()
-            panel_l = panel.lower()
-            param_default_str = (
-                param_default_str
-                + param_default_str_tmpl.format(panel_intf + "_panel_" + panel_l, panel_intf + "_PANEL_" + panel_u))
-
-    kconfig_panel_tuning_list = kconfig_config_str_tmpl.format(param_default_str, tuning_param)
-
-    return kconfig_panel_tuning_list
-
-
-def gen_panel_tuning_param_list(panel_param_list, tuning_param):
-    param_default_str = ""
-
-    for panel_param in panel_param_list:
-        panel_param_u = panel_param.upper()
-        panel_param_l = panel_param.lower()
-        param_default_str = (
-            param_default_str
-            + param_default_str_tmpl.format("MIPI_panel_" + panel_param_l, "MIPI_PANEL_" + panel_param_u))
-
-    kconfig_panel_tuning_param_list = kconfig_config_str_tmpl.format(param_default_str, tuning_param)
-
-    return kconfig_panel_tuning_param_list
-
-
 def main():
 
-    with open(build_helper.PANEL_LIST_PATH, "r", encoding="utf-8") as fp:
-        panel_list_json = json.load(fp)
-    panel_intf_list = panel_list_json['panel_list']
-    panel_param_list = panel_list_json['panel_param']
-    kconfig_panel_list = gen_panel_list(panel_intf_list)
-    kconfig_panel_tuning_param_list = (
-        gen_panel_tuning_list(panel_intf_list, "PANEL_TUNING_PARAM")
-        + gen_panel_tuning_param_list(panel_param_list['lane_num'], "PANEL_LANE_NUM_TUNING_PARAM")
-        + gen_panel_tuning_param_list(panel_param_list['lane_swap'], "PANEL_LANE_SWAP_TUNING_PARAM"))
+    panel_headers = get_panel_headers()
 
     kconfig = kconfig_tmpl.format(
-        kconfig_panel_list, kconfig_panel_tuning_param_list
+        gen_panel_list(panel_headers), ""
     )
 
     with open(build_helper.PANEL_KCONFIG_PATH, "w") as fp:
-- 
2.25.1

